{
  "name": "data_processing",
  "version": "1.0.0",
  "description": "process the csv data and write data to mysql",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "mysql": "^2.18.1"
  }
}
